requests>=2.31,<3
lxml>=5.0,<6
python-dotenv>=1.0,<2
pydantic>=2.6,<3
pydantic-settings>=2.2,<3
tenacity>=8.2,<9
schedule>=1.2,<2
PyYAML>=6.0,<7

# Lets pydantic resolve `X | Y` / `list[X]` style annotations on Python <3.10
# (e.g. the Python 3.8 required for Windows 7). No-op / not installed on 3.10+.
eval_type_backport>=0.2,<1; python_version < "3.10"
